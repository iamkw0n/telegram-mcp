{
  "name": "telegram-mcp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^1.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "latest",
    "telegram": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
